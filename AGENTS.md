# 仓库工作约束

## 校验策略

- 默认只执行语法级校验，不主动执行完整 `build` 或 `test`
- 允许的默认校验包括：Go 代码格式化、静态语法检查、前端类型检查、Vue SFC 语法校验
- 仅在以下情况才执行完整构建或测试：
  - 用户明确要求
  - 问题本身就是构建失败、测试失败或 CI 失败
  - 修改内容直接涉及构建链、测试框架或发布流程

## 变更原则

- 优先做最小必要修改
- 先定位根因，再修改代码
- 非用户明确要求时，不额外扩大改动范围
